# DeepSeek Orcana environment
# Copy this file to .env in the project where you run Orcana.

# Required for the default DeepSeek provider.
DEEPSEEK_API_KEY=sk-your-key-here

# Optional: DeepSeek Anthropic-compatible endpoint.
ANTHROPIC_BASE_URL=https://api.deepseek.com/anthropic

# Optional: force a registered model id.
# ORCANA_MODEL_OVERRIDE=deepseek-v4-pro

# Optional: set stricter cost behavior.
# ORCANA_COST_MODE=normal

# Optional: tune long-context memory checkpointing.
# ORCANA_M0_THRESHOLD_TOKENS=50000

# Optional: provider idle timeout in milliseconds.
# ORCANA_PROVIDER_IDLE_TIMEOUT_MS=60000

# Optional: override the default max rounds (default: 50).
# ORCANA_MAX_ROUNDS=100

# Optional: disable the TUI startup splash.
# ORCANA_TUI_SPLASH=off

# Optional: Exa semantic search API key (free: 1,000 searches/month).
# Get one at https://exa.ai — enables web_search without Docker.
# EXA_API_KEY=your-exa-key

# Optional: Jina Reader API key (free: 200 req/min, 10M tokens).
# Get one at https://jina.ai/reader — enables web_fetch Markdown extraction.
# JINA_API_KEY=your-jina-key
